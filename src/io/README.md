# Input/Output

This directory contains classes and functions for handling input and output operations.

## Key Components

- `FileLoader`: Loads data from files (OTB, XML, etc.).
- `IOMap`: Handles map loading and saving.
- `IOLoginData`: Manages player login data and database interactions.
- `Network`: Handles network communication with clients (protocols, connection).
